{
  "name": "kaels-kabbage.com",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-index": "cd IndexBuilder && dotnet run && cd ..",
    "build": "npm-run-all build-index optimise-assets update-latest-render update-carousel-links",
    "new": "hugo new --kind post-bundle",
    "start": "hugo server -D --poll 1000ms",
    "update-carousel-links": "cd render-link-builder && dotnet run --configuration Release ../content/Renders/index.md",
    "optimise-assets": "cd NewAssetOptimiser && dotnet run --configuration Release ../content",
    "update-latest-render": "cd SetLatestRender && dotnet run && cd .."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
